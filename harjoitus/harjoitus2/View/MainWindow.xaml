<Window x:Class="harjoitus2.View.MainWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:harjoitus2.View"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <TextBox Height="49" Text="{Binding Path = opettajanhuone.Numero, Mode=OneWay }" Width="160" Margin="48,23,310.4,248.8"/>
        <TextBox Height="49" Text="{Binding Path = esine1.Name, Mode=OneWay }" Width="160" Margin="48,23,310.4,248.8"/>
        <Image Source="{Binding esine1.Image, Mode=OneWay}" Width="232" Height="127" Margin="10,160,275,32"></Image>
    </Grid>
</Window>
