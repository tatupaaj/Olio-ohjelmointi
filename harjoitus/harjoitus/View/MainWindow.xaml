<Window x:Class="harjoitus.View.MainWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Button x:Name="button" Content="Button" HorizontalAlignment="Left" Height="50" Margin="290,46,0,0" VerticalAlignment="Top" Width="134" Click="button_Click"/>
        <TextBox Height="43" Text="{Binding Path=opettajanhuone.Name, Mode=OneWay}" Width="197" Margin="10,27,310,249"/>
        <TextBox Height="43" Text="{Binding Path=esine1.Name, Mode=OneWay}" Width="197" Margin="10,75,310,201"/>
        <Image Margin="0,168,310.4,-0.2">
            <Image.Source>
                <BitmapImage UriSource="pack://application:,,,/Resources/table.png"/>
            </Image.Source>
        </Image>
        <!-- <Button Name="AButton" Click="OnImageButtonClick" Margin="234,145,43.4,9.8" Background="{x:Null}" BorderBrush="{x:Null}">
            <Image Source="{Binding esine1.Image, Mode=OneWay}" Width="232" Height="127"></Image>
        </Button> -->
    </Grid>
</Window>
